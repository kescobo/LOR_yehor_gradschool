# Yehor grad school LOR

## Usage

By default, creates a letter with no signature -
use Adobe Acrobate or Docusign to add digital signature.

Or, if digital signing is not important,
add an image, and set the `signature` argument
to something like image("signature.png")`.
